# Assets

Arquivos-fonte de design (não consumidos diretamente pelo build). As versões
já prontas para uso (PNG nos tamanhos certos, favicon) ficam em
`frontend/public/`, geradas a partir do `icon.svg` aqui.
