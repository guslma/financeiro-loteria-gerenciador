# Deploy

Reservado para os arquivos de deploy self-hosted (Dockerfile, docker-compose.yml) a serem adicionados na Fase 7 do plano de evolução do app, para rodar em um servidor doméstico (ex.: ZimaOS).
