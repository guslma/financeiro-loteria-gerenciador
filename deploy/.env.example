# Copie para deploy/.env antes de rodar "docker compose up" a partir desta pasta.

# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="troque-este-valor"

# Definidos apenas no primeiro start (cria o usuário se ainda não existir).
SEED_USERNAME="admin"
SEED_PASSWORD="troque-esta-senha"
